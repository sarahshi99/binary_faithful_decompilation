int if_elseif_chain(int x) {
    // if-else if chain, 3 main branches
    if (x == 0) {
        return 10;
    } else if (x == 1) {
        return 20;
    } else if (x == 2) {
        return 30;
    } else {
        return -1;  // Default branch
    }
}
