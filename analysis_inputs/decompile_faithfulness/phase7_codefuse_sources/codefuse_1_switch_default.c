int switch_default(int op) {
    // switch with default
    switch (op) {
        case 1: return 100;
        case 2: return 200;
        case 3: return 300;
        default: return 0;  // All cases not covered
    }
}
